# setup configuration for installing package

from setuptools import setup

setup(
   name='webomakeup',
   version='1.0',
   description='Makeup recommendation web server',
   author='Sebastian Pietras',
   packages=['webmakeup']
)
