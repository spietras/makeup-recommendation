# setup configuration for installing package

from setuptools import setup

setup(
   name='automakeup',
   version='1.0',
   description='Makeup recommendation',
   author='Sebastian Pietras',
   packages=['automakeup']
)
